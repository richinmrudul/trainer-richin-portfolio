/**
 * Canonical public image URLs (files under `/public`).
 * Dimensions must match `public/images/richinheadshot.png` for stable layout hints.
 */
export const HERO_HEADSHOT = {
  src: "/images/richinheadshot.png",
  width: 1254,
  height: 1254,
  alt: "Richin Mrudul",
} as const;
