/**
 * Legacy slot — route art now lives in `RouteWorldLayer` (`/world/reference.webp`).
 * Kept as a no-op so imports can be removed gradually without churn.
 */
export function RouteBackground() {
  return null;
}
